import React from "react";

const Heading = ({ title }) => {
  return (
    <h2 className="text-5xl text-[#2c394c] font-[400] w-full text-center mb-8">
      {title}
    </h2>
  );
};

export default Heading;
