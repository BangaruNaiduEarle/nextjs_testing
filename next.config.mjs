/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  output: "export",
  // assetPrefix: "https://webdev.gitam.edu/nextpro/",
  // assetPrefix: "/nextjswebsite/",
  // basePath: "/nextpro",
  reactStrictMode: true,
};

export default nextConfig;
